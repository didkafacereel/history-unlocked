# Everything here that is regenerable or large stays out of a public repo.
# A day's source of truth is index.html + script.json + timings.json; the
# images are public-domain files re-fetched from Wikimedia Commons, the audio
# is re-synthesised locally, and renders are outputs.
*/renders/
*/snapshots/
*/audio/
*/narration.wav
*/assets/*.jpg
*/assets/*.jpeg
*/assets/*.png
*/.hyperframes/
